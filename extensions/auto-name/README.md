# Vendored auto-name extension

Vendored from `@ryan_nookpi/pi-extension-auto-name@0.3.3` (`Jonghakseo/pi-extension`, package metadata: MIT) so the installed behavior includes the live English naming prompt instead of an untracked patch to Pi's npm cache.
