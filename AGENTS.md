# Hasan's Pi instructions

## Working style

- Follow the nearest project-local `AGENTS.md` when one exists.
- Keep responses concise and lead with the verdict.
- Preserve unrelated work in shared or dirty repositories.
- Use `pnpm` unless a project is explicitly legacy npm.
- Do not delegate work unless Hasan explicitly asks for it or invokes `/orchestrate`.

## Research

- Use Context7 for current library documentation and API setup.
- Use Firecrawl search for general web research and scrape only when the full page is needed.

## Verification and safety

- Do not call work done until the relevant behavior has been executed and observed.
- Report status as done or not done; name the exact missing proof when blocked.
- Never touch production or deploy without explicit approval in the active conversation.
- Use localhost for local development servers.
- Use `generate_image` for Codex-native raster image generation.
